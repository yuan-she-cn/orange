{
  "compilerOptions": {
    // JavaScript 标准库
    "lib": ["ES2015", "DOM"],
    // 全局类型声明
    "types": ["@jest/types", "@types/jest"],
    // 输出模块系统版本，影响模块导入导出语法
    "module": "es2015",
    // 输出目标代码版本，影响语言特性语法
    "target": "es2015",

    // 生成源码映射文件(.js.map)
    "sourceMap": true,
    // 生成类型声明文件(.d.ts)
    "declaration": true,
    // 生成类型映射文件(.d.ts.map)
    "declarationMap": true,
    // 打包时移除注释
    "removeComments": true,

    // 确保单个文件为独立模块，不依赖其他文件
    "isolatedModules": true,
    // 设置单个文件模式，模块还是脚本。force 强制单个文件为独立模块，避免类型污染
    "moduleDetection": "force",
    // 设置查找和解析模块策略，bundler 为打包器策略
    "moduleResolution": "bundler",

    // 启用严格的类型检查模式
    "strict": true,
    // 严格区分类型导入和值导入，即 import type 和 import
    "verbatimModuleSyntax": true,
    // 跳过依赖库的类型检查
    "skipLibCheck": true,
    // 可选属性允许省略，但不允许 undefined
    "exactOptionalPropertyTypes": true,
    // 检查可能为 undefined 的索引访问，如：array[number]，object["attribute"]
    "noUncheckedIndexedAccess": true,
    // 强制使用索引访问未知属性，禁止使用点语法访问未知属性
    "noPropertyAccessFromIndexSignature": true,
    // 检查未使用的变量
    "noUnusedLocals": true,
    // 检查未使用的参数
    "noUnusedParameters": true,
    // 当函数结果不为 void 时，检查函数必须有返回结果
    "noImplicitReturns": true,
    // 重写方法时必须使用 override 关键字
    "noImplicitOverride": true,
    // 检查 switch case 穿透
    "noFallthroughCasesInSwitch": true
  }
}
